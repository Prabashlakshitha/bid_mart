/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next 16 removed the `eslint` config key — linting is run separately now
  // (see https://nextjs.org/docs/app/api-reference/cli/next#next-lint-options).
};

module.exports = nextConfig;
